<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchoolService.Pages.Worker.MainPages.WorkerProfile">
    <ContentPage.Content>
        
        <VerticalStackLayout 
            Spacing="10"
            Margin="10">
            <Button 
                Text="Resume"
                Clicked="ResumeClicked"
                Style="{StaticResource ProfileButton}"/>
            <Button 
                Text="BookMarks"
                Clicked="BookMarksClicked"
                Style="{StaticResource ProfileButton}"/>
            <!-- <Button  -->
            <!--     Text="Resume" -->
            <!--     Clicked="ResumeClicked" -->
            <!--     Style="{StaticResource ProfileButton}"/> -->

        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>